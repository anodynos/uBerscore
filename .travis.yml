language: node_js

node_js:
  - 0.10
  - 0.12
  - 4
  - 5

before_script:
  - npm install -g grunt-cli
  - npm install -g bower
  - npm install -g mocha
  - npm install -g mocha-phantomjs
  - bower install

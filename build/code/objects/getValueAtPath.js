// Generated by uRequire v0.3.0alpha22
(function () {
  var __isAMD = (typeof define === 'function' && define.amd),
    __isNode = (typeof exports === 'object'),
    __isWeb = !__isNode;
(function (root,factory) {
  if (typeof exports === 'object') {
   var nr = new (require('urequire').NodeRequirer) ('objects/getValueAtPath', module, __dirname, '.');
   module.exports = factory(nr.require, exports, module, nr.require('lodash'), nr.require('../agreement/isAgree'));
 } else if (typeof define === 'function' && define.amd) {
     define(['require', 'exports', 'module', 'lodash', '../agreement/isAgree'], factory);
 }
})(this,function (require, exports, module, _, isAgree) {
  // uRequire: start body of original nodejs module
var getValueAtPath, _;

_ = require("lodash");

getValueAtPath = function(o, path, separator) {
    var p, _i, _len;
    if (separator == null) {
        separator = "/";
    }
    if (!_.isArray(path)) {
        if (_.isString(path)) {
            path = path.split(separator);
        } else {
            throw "_B.getValueAtPath Error: invalid path: " + path;
        }
    }
    for (_i = 0, _len = path.length; _i < _len; _i++) {
        p = path[_i];
        if (!p) {
            continue;
        }
        if (!_.isObject(o)) {
            return void 0;
        }
        o = o[p];
    }
    return o;
};

module.exports = getValueAtPath;
// uRequire: end body of original nodejs module


return module.exports;
})
})();
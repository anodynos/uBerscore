// Generated by uRequire v0.6.0 - template: 'UMD'
(function () {
  var __isAMD = (typeof define === 'function' && define.amd),
    __isNode = (typeof exports === 'object'),
    __isWeb = !__isNode;


(function (root,factory) {
  if (typeof exports === 'object') {
   var nr = new (require('urequire').NodeRequirer) ('blending/traverse', module, __dirname, '.');
   module.exports = factory(nr.require, exports, module, nr.require('lodash'), nr.require('../agreement/isAgree'));
 } else if (typeof define === 'function' && define.amd) {
     define(['require', 'exports', 'module', 'lodash', '../agreement/isAgree', './Blender'], factory);
 }
}).call(this, this,function (require, exports, module, _, isAgree) {
  
// uRequire v0.6.0: START body of original nodejs module
  var Blender, traverse;
  Blender = require("./Blender");
  traverse = function (data, callback) {
    var dummy, recurse, traverseBlender;
    dummy = {};
    recurse = true;
    traverseBlender = new Blender([{
        order: ["src"],
        "[]": "traverse",
        "{}": "traverse",
        "->": "traverse",
        "*": Blender.SKIP,
        traverse: function (prop, src, dst, blender) {
          if (src[prop] !== data) {
            recurse = callback.call(this, prop, src, blender);
          }
          if (recurse !== false) {
            this.blend(dummy, src[prop]);
          }
          return this.SKIP;
        }
      }], { debugLevel: 0 });
    return traverseBlender.blend(dummy, data);
  };
  module.exports = traverse;
// uRequire v0.6.0: END body of original nodejs module


return module.exports;
})
}).call(this);
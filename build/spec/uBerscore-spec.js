// Generated by uRequire v0.3.0alpha15
(function (root, factory) {
  if (typeof exports === 'object') {
    var nr = new (require('urequire').NodeRequirer) ('uBerscore-spec', __dirname, '.');
    module.exports = factory(nr.require, exports, module);
  } else if (typeof define === 'function' && define.amd) {
      define(['require', 'exports', 'module', 'chai', 'uBerscore'], factory);
  }
})(this, function (require, exports, module) {
 var isWeb = (typeof define === 'function' && define.amd), isNode = !isWeb;

// uRequire: start body of original nodejs module
var assert, chai, expect, _uB;

chai = require("chai");

assert = chai.assert;

expect = chai.expect;

_uB = require("uBerscore");

if (isWeb) {
    describe("uBerscore rootExports:", function() {
        it("registers globals _B & uBerscore", function() {
            expect(_B).to.equal(_uB);
            return expect(uBerscore).to.equal(_uB);
        });
        return it("noConflict() returns module & sets old values to globals _B & uBerscore", function() {
            expect(_B.noConflict()).to.equal(_uB);
            expect(_B).to.equal("old _B");
            return expect(uBerscore).to.equal("old uBerscore");
        });
    });
}
// uRequire: end body of original nodejs module


return module.exports; 
});
// Generated by uRequire v{NO_VERSION}
(function () {
  var __isAMD = (typeof define === 'function' && define.amd),
    __isNode = (typeof exports === 'object'),
    __isWeb = !__isNode;
(function (root,factory) {
  if (typeof exports === 'object') {
   var nr = new (require('urequire').NodeRequirer) ('blending/deepCloneDefaults-spec', module, __dirname, '.');
   module.exports = factory(nr.require, exports, module, nr.require('chai'), nr.require('lodash'), nr.require('uberscore'), nr.require('../spec-data'));
 } else if (typeof define === 'function' && define.amd) {
     define(['require', 'exports', 'module', 'chai', 'lodash', 'uberscore', '../spec-data'], factory);
 }
})(this,function (require, exports, module, chai, _, _B, data) {
  // uRequire: start body of original nodejs module
var arrInt, arrInt2, arrStr, assert, bundle, bundle_project_team, expect, obj, project, team, _ref;

assert = chai.assert;

expect = chai.expect;

_ref = _.clone(data, true), project = _ref.project, team = _ref.team, bundle = _ref.bundle, bundle_project_team = _ref.bundle_project_team, obj = _ref.obj, arrInt = _ref.arrInt, arrInt2 = _ref.arrInt2, arrStr = _ref.arrStr;

describe("deepCloneDefaults:", function() {
    it("more 'specific' options eg. project, merged (taking precedence) to more 'team' defaults", function() {
        var result;
        result = _B.deepCloneDefaults(project, team);
        return expect(result).to.deep.equal({
            enabled: true,
            bundleRoot: "/team/project",
            compilers: {
                "rjs-build": "project-rjs-build"
            }
        });
    });
    it("many defaults", function() {
        var result;
        result = _B.deepCloneDefaults(bundle, project, team);
        return expect(result).to.deep.equal(bundle_project_team);
    });
    return it("Original objects not mutated", function() {
        expect(bundle).to.deep.equal(data.bundle);
        expect(project).to.deep.equal(data.project);
        return expect(team).to.deep.equal(data.team);
    });
});
// uRequire: end body of original nodejs module


return module.exports;
})
})();